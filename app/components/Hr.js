import React from 'react';
import { View } from 'react-native';
import { Common, Color } from '../utils/Theme';

export default () =>
  (
    <View
      style={{ height: Common.hrHeight, backgroundColor: Color.background }}
    />
  );

